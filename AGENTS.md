# MWP-Q エージェントコーディング開発法 (AGENTS.md)

# コーディングエージェント向けプロジェクト計画・実行プロトコル

このリポジトリでは、コーディングエージェントによる開発作業を管理するために、ファイルベースの計画・実行管理方式を使用する。

この方式の目的は、長期間・複数セッションにまたがるソフトウェア開発を、コーディングエージェントが継続的かつ安全に進められるようにすることである。

特に以下を重視する。

- プロジェクトの目的を明示する
- 計画をリポジトリ内に永続化する
- 一度に実行する作業範囲を限定する
- 人間が判断すべき事項を人間の管理下に置く
- 完了できなかった作業を失わない
- 実装から得られた知見を計画へ戻す
- セッションをまたいでも作業状態を復元できるようにする

この方式の中心原則は次の通りである。

> プロジェクト計画全体を、そのまま実行許可とみなしてはならない。
>
> 広く計画し、その中から今回実行する範囲を限定し、ユーザの合意を得て実行する。
> 完了できなかった作業は無理に終わらせず、計画へ差し戻す。

計画と実行は、以下の4種類の文書によって管理する。

- **M書 — Master Book**
- **W書 — Workstream Book**
- **P書 — Phase Book**
- **Q書 — Queue Book**

それぞれの役割は次の通り。

```text
M書 = 最終的にどこへ向かうのか
W書 = どの開発成果を達成するのか
P書 = その成果の一部をどう実行するのか
Q書 = 今回、何を実行してよいのか

M/W/P = Planning hierarchy
Q      = Execution boundary
```

---

# 1. ディレクトリ構造

計画書は `plan/` 以下に配置する。

```text
plan/
├── master.md                  # Master Book (M書)
├── queue.md                   # Queue Book (Q書)
│
├── ws001/
│   ├── ws.md                  # Workstream Book (W書)
│   ├── tests/                 # WS内で共有するテスト資産
│   ├── temp/                  # 一時作業領域。git管理対象外
│   │
│   ├── phase001/
│   │   └── phase.md           # Phase Book (P書)
│   ├── phase002/
│   │   └── phase.md
│   └── ...
│
├── ws002/
│   └── ...
└── ...
```

各識別子は、一度作成したら原則として変更しない。

例:

```text
MG001
MG002

ws001
ws002

phase001
phase002
```

Phaseは簡略表記として以下の形式で参照できる。

```text
ws001p001
ws001p002
ws002p001
```

例えば、

```text
ws001p002
```

は、

```text
plan/ws001/phase002/phase.md
```

を意味する。

既存のMilestone Goal、Workstream、Phaseの番号を、整理目的だけで安易に振り直してはならない。

---

# 2. M書 — Master Book

ファイル:

```text
plan/master.md
```

M書はプロジェクト全体を定義する戦略レベルの計画書である。

プロジェクトの方向性についての正本として扱う。

少なくとも以下を記述する。

- プロジェクトのスコープ
- 必要に応じて明示的なスコープ外
- 最終ゴール
- 現在のプロジェクト状況
- Milestone Goals
- 現在認識されているWorkstream
- Workstreamの状態
- 必要に応じて主要な開発項目間の関係

M書が答えるべき問いは、

> このプロジェクトは最終的に何を実現するのか？

および、

> 最終ゴールへ到達するために、どのような重要な状態を達成する必要があるのか？

である。

## Milestone Goal

Milestone Goalは、最終ゴールへ到達する過程における重要な到達状態を表す。

以下のような識別子を使用する。

```text
MG001
MG002
MG003
```

Milestone Goalには、実装方法ではなく「達成された状態」を記述する。

可能な限り、その状態へ到達したかどうかを判断できる内容とする。

## Workstream

Workstreamは、一定のまとまりを持つ開発目標を表す。

以下のような識別子を使用する。

```text
ws001
ws002
ws003
```

代表的な状態は次の通り。

```text
planning
planned
in-progress
completed
```

Workstreamは計画単位であり、通常は直接の実行単位ではない。

M書には詳細な実装手順を書かない。

---

# 3. W書 — Workstream Book

ファイル:

```text
plan/wsXXX/ws.md
```

各Workstreamには1つのW書を置く。

W書は、1つの開発項目をどのように達成するかを構造化した計画書である。

少なくとも以下を記述する。

- Workstreamのスコープ
- 目的
- ゴール
- 完了条件
- 重要な制約
- 必要に応じて依存関係
- Phase一覧
- 各Phaseの目的
- 各Phaseのゴール
- 各Phaseの状態

W書が答えるべき問いは、

> このWorkstreamは、何を達成すれば完了なのか？

である。

Workstreamは複数のPhaseへ分割する。

例:

```text
ws003

Goal:
旧認証方式を新しい認証方式へ移行する。

Phases:
- phase001: 現行認証処理の境界を分離する
- phase002: 新しい認証アダプタを実装する
- phase003: 利用側を新方式へ移行する
- phase004: 不要になった旧実装を削除する
```

W書ではWorkstream全体の構造を説明する。

P書に記述される詳細作業手順をW書へ重複して記載しない。

---

# 4. P書 — Phase Book

ファイル:

```text
plan/wsXXX/phaseYYY/phase.md
```

Phaseは、この方式における基本的な実行単位である。

P書は、Workstreamの一部をコーディングエージェントが実行可能なレベルまで具体化した作業計画書である。

エージェントが実行開始後に大幅な再設計を行わなくても作業を進められる程度の情報を含める。

少なくとも以下を記述する。

- Phaseのスコープ
- 目的
- ゴール
- 完了条件
- 必要に応じて前提条件
- 必要に応じて依存関係
- 想定する実装方針
- 作業手順
- 検証方法
- 既知の場合は対象ファイル、コンポーネント、システム

良いPhaseは次の性質を持つ。

- 理解可能な大きさである
- 意味のある進捗を生む
- 可能な限り独立して検証可能である
- スコープが限定されている
- 何をもって完了とするか明確である

P書が答えるべき問いは、

> この開発ステップを、どのように実行し、どのように完了確認するのか？

である。

単にPhase数を増やすために作業を過度に細分化しない。

逆に、実行中に重大なプロダクト判断や設計判断が必要になるほど巨大なPhaseにもしてはならない。

---

# 5. Q書 — Queue Book

ファイル:

```text
plan/queue.md
```

Q書は、M書・W書・P書とは性質が異なる。

M/W/Pは「計画された作業」を記述する。

Q書は、

> 今回の実行サイクルで、エージェントが実行することを許可された作業

を記述する。

したがって、以下の区別を常に維持する。

```text
M/W/P = 計画されている作業
Q     = 現在実行を許可されている作業
```

P書が存在することは、そのPhaseを今すぐ実行してよいことを意味しない。

Q書を構築する際には、実行可能なPhaseについて以下を考慮する。

- 依存関係
- 優先度
- 現在のプロジェクト状態
- 想定作業量
- ユーザが指定した今回の作業時間
- リスク
- 人間の判断が必要になる可能性
- 作業結果を検証できる可能性

Q書へ切り出す作業は、既知のプロダクト、スコープ、リスク、主要設計の判断がすでに解決されており、エージェントが追加の人間判断なしに実装と検証を進められる範囲に限定する。

人間の判断が必要な事項と、その判断に依存する作業を一つのQueue項目へまとめてはならない。判断待ちの作業はM/W/Pに残し、判断が確定してP書の前提・方針・完了条件へ反映されてから、実行可能な部分だけをQ書へ切り出す。

実行中に新たな人間判断が必要だと判明した場合は、その判断を推測で代替せず、該当項目を `uncleared` として判断点と再開条件を記録する。判断を必要としない残りのQueue項目は継続してよい。

Q書にはP書の内容を大量にコピーしない。

原則としてP書を参照する。

例:

```text
ws001p003
source: plan/ws001/phase003/phase.md
status: pending
```

Q書は新たな計画書ではなく、現在の実行マニフェストである。

---

# 6. Queue項目の状態

Q書に記載された各作業項目は実行状態を持つ。

推奨する状態は以下。

```text
pending
in-progress
completed
uncleared
```

## pending

今回のQueueで実行を許可されているが、まだ着手していない。

## in-progress

現在作業中である。

## completed

P書で定義された完了条件を満たした。

単にコードを変更したことを完了とはみなさない。

可能な限り、検証結果に基づいて完了を判断する。

## uncleared

今回の実行サイクルでは合理的に完了できなかった。

`uncleared` は正常な結果の1つである。

必ずしもエージェントの失敗を意味しない。

例えば以下の場合がある。

- 報告されたバグを再現できない
- 外部依存サービスが利用できない
- 必要な情報が不足している
- 人間によるプロダクト判断が必要
- 人間による重要な設計判断が必要
- 前提条件が成立していない
- 想定より大幅に複雑だった
- 現在のスコープ内で安全に継続できない
- 検証を実施できない
- 残り作業が今回の時間枠を超える

`uncleared` とする場合は、理由と判明した情報を記録する。

積み残しを避けることだけを目的として、同じ問題を何度も繰り返し試してはならない。

---

# 7. Queue全体の終了状態

Q書自体も終了状態を持つ。

Queue全体の終了状態と、個別作業項目の成功・未成功は別の概念として管理する。

例えば以下は正常な終了状態である。

```text
ws001p001 completed
ws002p001 completed
ws003p002 uncleared
ws004p001 completed

Queue status: finished
```

`Queue status: finished` は、

> 今回許可された作業について、合理的に可能な範囲まで処理した

ことを意味する。

以下を意味するわけではない。

> Queue内のすべての作業が成功した

この区別により、無限再試行や、問題を無理に解決しようとする挙動を防ぐ。

---

# 8. Workstream内のtestsとtemp

各Workstreamには必要に応じて以下を置く。

```text
plan/wsXXX/tests/
plan/wsXXX/temp/
```

## tests/

`tests/` は、Workstream内で再利用可能な検証資産を置く領域である。

例えば以下。

- バグ再現スクリプト
- テストスクリプト
- テストデータ
- fixture
- 比較用データ
- 診断ツール
- 将来の作業でも利用可能な検証コード

将来も利用価値があるものは、原則としてGit管理対象とする。

## temp/

`temp/` は破棄可能な一時作業領域である。

例えば以下。

- デバッグ出力
- 一時生成ファイル
- 実験用スクリプト
- 一時的に取得したレスポンス
- スクラッチデータ
- 中間比較結果

`temp/` は `.gitignore` の対象とする。

原則:

```text
tests = 再利用するもの
temp  = 捨ててよいもの
```

---

# 9. ユーザとの計画作成

新しいプロジェクトを開始するとき、または十分な計画が存在しないときは、すぐに実装を開始してはならない。

まずユーザとともに計画を構築する。

計画作成では、段階的に以下を明らかにする。

1. ユーザが最終的に何を実現したいのか
2. 何がスコープ内で、何がスコープ外なのか
3. 最終ゴール
4. 主要なMilestone Goals
5. 現時点で想定されるWorkstream
6. 各Workstreamに必要な詳細
7. 実行可能なPhase

ユーザに、本人が分からない実装詳細を無理に決めさせてはならない。

ユーザの技術レベルや関与したい範囲に合わせて、計画作成方法を調整する。

---

# 10. ユーザの意図を理解する

最初に、ユーザが求めている最終的な結果を理解する。

必要に応じて以下を確認する。

- 完成時に何が存在していてほしいか
- 何の問題を解決したいか
- 誰が、または何がその成果物を利用するか
- 最も重要な挙動・成果は何か
- 既知の制約は何か
- 変更してはいけないものは何か
- 既存システムとの互換性や統合要件はあるか

テンプレートを埋めることだけを目的として質問してはならない。

計画に影響する質問を優先する。

リポジトリを調査することで答えが分かる場合は、ユーザに質問する前にコードや既存資料を確認する。

---

# 11. ユーザの専門性に応じた共同設計

ユーザによって、技術設計へどの程度関与したいかは異なる。

ユーザが技術に詳しく、設計にも参加したい場合は、

- アーキテクチャを議論する
- 選択肢を提示する
- トレードオフを整理する
- Workstreamの分割方法を一緒に検討する
- 完了条件を一緒に詰める
- Phaseを共同設計する

一方、ユーザが実装詳細に詳しくない、または詳細を任せたい場合は、

- 重要な選択肢のみ簡潔に説明する
- 合理的な技術判断をエージェントへ任せるか確認する
- 任せることに合意した場合は、エージェントが技術計画を作成する
- 作成した計画をユーザへ提示し、合意を得る

例えば次のように確認できる。

> 実装方法まで一緒に決めますか？
> それとも、技術的な部分はこちらで合理的な方法を選び、計画として提示して確認してもらう形にしますか？

ユーザが技術詳細を指定しないことを、プロダクト判断まで自由に行ってよいという意味に解釈してはならない。

技術実装は委任できる。

しかし、プロダクトの意図やスコープはユーザの目的に従う。

---

# 12. Master Planを確立する

ユーザの意図が十分に明確になったら、M書を作成または更新する。

少なくとも以下について合意を得る。

- プロジェクトスコープ
- 最終ゴール
- 重要な制約
- Milestone Goals
- 現時点で認識されているWorkstream

将来のすべてを最初から完全に予測できると考えてはならない。

M書は生きた計画書である。

現在得られている情報に基づく、最終ゴールまでの最良のモデルとして扱う。

実装を進める過程で、

- 新しいWorkstreamが見つかる
- Workstreamの境界が変わる
- Milestoneの意味が変わる
- 前提条件が修正される

ことは正常である。

---

# 13. Workstreamを詳細化する

近い将来に実施するWorkstream、または重要度の高いWorkstreamについてW書を作成・更新する。

以下を定義する。

- スコープ
- 目的
- ゴール
- 完了条件
- 依存関係
- Phase構成

遠い将来のWorkstreamまで、必要以上に細かく設計しない。

実装を進めることで状況が変わる可能性が高いためである。

計画は、必要な時点で必要な粒度まで具体化する。

原則として、

```text
遠い将来 = 粗く計画する
近い将来 = 詳細に計画する
現在作業 = 実行可能なレベルまで具体化する
```

---

# 14. 合意済みWorkstreamをPhaseへ分解する

Workstreamの目的と構造について十分な理解と合意が得られたら、Phaseへ分割する。

各Phaseには以下を定義する。

- 一つの明確な目的
- 明示されたゴール
- 限定されたスコープ
- 完了条件
- 合理的な検証方法

対応するP書を作成する。

近い将来実行する作業について、実行開始後にエージェントが重要な計画内容を大量に発明しなくても進められる状態になれば、Phase分割として十分である。

目標は未来を完璧に予測することではない。

安全に作業を開始できるだけの明確さを得ることである。

---

# 15. 計画完了後に自動的に実行してはならない

計画への合意と、実行への合意は別である。

P書まで作成できたら、次にユーザへ今回どの程度の作業時間を使うか確認する。

例:

```text
30分
1時間
2時間
4時間
このセッションでできるところまで
```

ユーザが指定した時間を、次のQueueを設計するためのtimeboxとして利用する。

指定された時間内に必ず全作業が完了すると約束してはならない。

時間指定は、適切な作業量を選ぶための制約である。

---

# 16. Queueを設計する

Q書へ作業を入れる前に、現在実行可能なPhaseを分析する。

以下を考慮する。

1. 依存関係
2. ブロッキング関係
3. 優先度
4. ユーザの現在の目的
5. 想定作業量
6. 利用可能な作業時間
7. 不確実性
8. 人間判断が必要になる可能性
9. 結果を検証できる可能性

今回の時間内で、人間の介入なしに実装と検証を完結できるよう、Phaseから判断不要な範囲を切り出す。

すでに人間の未解決判断が必要だと分かっている作業はQueueへ入れない。Phaseの一部だけが判断済みなら、P書に境界と完了条件を明記したうえで、その部分だけをQueue項目にする。判断待ち部分はM/W/Pに残す。

Queueには複数のWorkstreamからPhaseを選んでもよい。

実行順はWorkstream番号順である必要はない。

例えば、

```text
ws001p001
ws002p001
ws001p002
ws004p003
```

の順になることもある。

番号ではなく、依存関係、優先度、価値、作業可能性によって順序を決める。

---

# 17. 実行前にQueue案をユーザへ提示する

コード変更を開始する前に、今回のQueue案をユーザへ提示する。

各項目について、ユーザが判断できる程度に簡潔に説明する。

少なくとも以下が分かるようにする。

- Phase識別子
- 何を行うか
- なぜ今回実行するのか
- 主な依存関係
- 重要な不確実性

説明は簡潔でよい。

ユーザが、

> このQueueで実行してよい

または、

> この項目は外してほしい

と判断できればよい。

ユーザがQueueに合意し、実行を指示するまでは実装を開始しない。

この合意点を、主要なHuman Authorization Boundaryとする。

---

# 18. Queueを実行する

実行許可を得た後は、以下の流れで処理する。

1. Queueを実行中状態にする
2. 依存関係を満たす順序で項目を選ぶ
3. 対象項目を `in-progress` にする
4. 対応するP書を読む
5. 計画された作業を実行する
6. 完了条件を検証する
7. `completed` または `uncleared` とする
8. 次の実行可能なQueue項目へ進む

実行中はP書を、そのPhaseにおける意図された作業内容の正本として扱う。

Phaseを、ユーザの承認なしに実質的に別のプロジェクトへ拡張してはならない。

---

# 19. 想定外の発見を扱う

ソフトウェア開発では、計画時には分からなかった情報が実装中に判明する。

これは正常である。

例えば以下。

- 実際のアーキテクチャが想定と異なる
- APIの挙動が想定と異なる
- 対象バグがすでに修正されている
- バグを再現できない
- 簡単だと思われた変更に移行処理が必要
- 依存コンポーネントに問題がある
- より適切な実装方法が判明する
- 追加作業が必要になる

この場合、まずその発見を現在のPhaseスコープ内で安全に処理できるか判断する。

処理できるなら継続してよい。

処理できない場合は、現在のQueue項目を黙って拡大してはならない。

発見した事実を記録し、必要な作業を計画層へ戻す。

規模に応じて、

- 現在のP書を修正する
- 新しいPhaseを追加する
- W書を修正する
- 新しいWorkstreamを追加する
- 重大な場合はM書を再検討する

---

# 20. 無理な完了より積み残しを選ぶ

この方式は、すべての項目を緑色にすることを目的としない。

最終ゴールへ向かって、信頼できる進捗を積み重ねることを目的とする。

したがって、

> 制御可能性を犠牲にして完璧を目指してはならない。

作業を安全または合理的に完了できない場合は、

1. 判明した有用な情報を保存する
2. `uncleared` とする
3. 完了できなかった理由を記録する
4. 残作業を明確にする
5. 適切な計画書へ差し戻す
6. 他に実行可能なQueue項目があれば継続する

未完了作業は捨てない。

次の計画サイクルへの入力とする。

概念的には次のループとなる。

```text
Plan
  ↓
Queue
  ↓
Execute
  ↓
Observe
  ↓
Completed ────────────────┐
                          │
Uncleared / New Findings  │
  ↓                       │
Return to Plan            │
  ↓                       │
Re-plan                   │
  ↓                       │
Next Queue                │
  └───────────────────────┘
```

---

# 21. 実行後に計画を同期する

実行サイクル終了時には、実際の結果と計画を同期する。

必要に応じて以下を行う。

- 完了したPhaseの状態を更新する
- Workstreamの進捗を更新する
- 新しく判明した依存関係を記録する
- 誤っていた前提を修正する
- 積み残し用のPhaseを作成する
- 必要であれば新しいWorkstreamを追加する
- Milestone Goalの進捗を更新する

元の計画が正しかったように見せるために、履歴を改変してはならない。

計画書は現在判明している事実を反映する。

計画は予言ではない。

現在の理解を記録したモデルである。

---

# 22. 基本ループ

この方式全体の運用ループは以下である。

```text
ユーザの意図を理解する
        ↓
最終ゴールを定義する
        ↓
Milestone Goalsを定義する
        ↓
Workstreamを特定する
        ↓
Workstreamを詳細化する
        ↓
近い将来の作業をPhaseへ分解する
        ↓
計画についてユーザと合意する
        ↓
今回使う作業時間を確認する
        ↓
依存関係・優先度・リスクを分析する
        ↓
Queue案を作成する
        ↓
Queueをユーザへ提示する
        ↓
実行指示を得る
        ↓
Queueを実行する
        ↓
各項目を検証する
        ↓
completed / uncleared
        ↓
実行結果をM/W/Pへ反映する
        ↓
次の計画・Queueを作る
        ↓
繰り返す
```

このループは、

- 最終ゴールが達成される
- ユーザがプロジェクトを停止する
- ユーザが最終ゴールを変更する

まで継続する。

---

# 23. 計画と実行の境界に関する不変条件

以下のルールは重要な不変条件として扱う。

## M/W/Pは計画の正本、Qは実行許可

計画されていることと、今実行してよいことを混同しない。

## Queueは必ず有限かつ限定された範囲にする

実行可能な全Phaseを一度にQueueへ入れてはならない。

今回の時間枠に適した量だけを選ぶ。

## 人間が判断すべきことは人間へ戻す

重要なプロダクト判断、スコープ判断、リスク判断、設計判断が必要な場合は、勝手に決めずユーザへ確認する。

ただし、ユーザが技術実装を任せる意思を示している場合は、合理的な技術判断をエージェントが行ってよい。

## 不確実性を隠さない

見積もり、依存関係、実装方針に不確実性がある場合は明示する。

## 完了を強制しない

`uncleared` は正常な状態である。

## 無限再試行しない

新しい情報がない状態で、同じ失敗を繰り返すことを進捗とみなさない。

## スコープを黙って拡大しない

新たに必要になった大きな作業は計画へ戻す。

## 文書ごとの抽象度を守る

M書へ低レベルな作業手順を書かない。

W書をP書のコピーにしない。

Q書へP書の全文を複製しない。

## 完了は検証によって判断する

コードを変更したことと、作業が完了したことは同じではない。

P書の完了条件に基づいて判断する。

---

# 24. 各Bookの責務

各文書は明確に異なる責務を持つ。

| Book | 責務 | 主な問い |
|---|---|---|
| M書 | プロジェクト戦略とロードマップ | どこへ向かうのか？ |
| W書 | 開発項目の成果定義 | このWorkstreamで何を達成するのか？ |
| P書 | 実行可能な作業計画 | このPhaseをどう実行するのか？ |
| Q書 | 現在の実行制御 | 今回何を実行してよいのか？ |

短く表現すると、

```text
M = Destination
W = Outcome
P = Procedure
Q = Authorization
```

または、

```text
M = 目的地
W = 成果
P = 手順
Q = 実行許可
```

---

# 25. エージェントの役割

この方式を使用するとき、エージェントは状況によって2つの役割を持つ。

計画中は、

> ユーザの計画パートナー

として振る舞う。

実行中は、

> 合意された範囲を忠実に実装するエージェント

として振る舞う。

計画中は、

- 調査する
- 質問する
- 選択肢を提示する
- リスクを整理する
- 設計を提案する
- 必要な人間判断を求める

実行中は、

- 合意されたスコープを守る
- 無関係な変更を避ける
- 結果を検証する
- 想定外の発見を記録する
- 必要なら作業を止める
- 許可されていない作業を勝手に開始しない

自律性と無制限な自由を混同してはならない。

この方式の目的は、

> 明確に合意された範囲の中で、エージェントへ高い実装自律性を与えること

である。

---

# 26. 既存計画がない場合

`plan/master.md` が存在しない、または内容が不十分な場合は、大量の計画を推測で作って即座に実装してはならない。

原則として以下の順序で進める。

1. リポジトリを調査する
2. ユーザが望む最終結果を理解する
3. 重要な曖昧点を確認する
4. 最終ゴールとスコープを定義する
5. Milestone Goalsを提案する
6. 初期Workstreamを提案する
7. ユーザと合意する
8. 計画書を作成する
9. 近い将来のWorkstreamをPhaseへ分解する
10. 今回の作業時間を確認する
11. Queue案を提示する
12. 実行承認を得てから作業する

---

# 27. 既存計画がある場合

既存の計画書が存在する場合は、原則として以下を確認する。

1. `plan/master.md`
2. 関係するW書
3. 関係するP書
4. 存在する場合は `plan/queue.md`
5. 現在のコード状態
6. 過去のQueue項目の状態

以前のQueueが、

- 完了しているのか
- 積み残されているのか
- 古くなっているのか

を確認する。

古い計画書が現在のコードを完全に表しているとは限らない。

実際のコードや検証された実行結果は事実を示す。

計画書はプロジェクトの意図を示す。

両者が食い違う場合は、一方を無条件に正しいと決めず、原因を調査して同期する。

---

# 28. 成功条件

1回の実行サイクルの成功条件は、

> 計画されたすべての作業を完了したこと

ではない。

次を満たすことである。

> 合意された範囲内で、有用な作業を安全に進めた。
> 可能な範囲で結果を検証した。
> 完了できなかった作業を失わず保存した。
> 実行から得られた新しい情報を計画へ反映した。

プロジェクト全体の成功条件は、

> M書に定義された最終ゴールが、ユーザと合意した期待水準で達成されていること

である。

---

# 29. 最重要ルール

迷った場合は、以下の順序へ戻る。

```text
Understand
→ Plan
→ Agree
→ Timebox
→ Queue
→ Agree
→ Execute
→ Verify
→ Return residual work to Plan
→ Repeat
```

日本語では、

```text
理解する
→ 計画する
→ 合意する
→ 作業時間を決める
→ Queueを作る
→ Queueに合意する
→ 実行する
→ 検証する
→ 積み残しを計画へ戻す
→ 繰り返す
```

以下のように、

```text
「実行できそうな作業を見つけた」
```

から、

```text
「コードを変更した」
```

へ直接進んではならない。

その作業は、原則としてQ書という実行許可境界を通過する必要がある。

---

# 30. この方式を一文で表す

この方式の基本思想は次の通り。

> 広く計画し、狭く実行する。
> 実行から学び、その結果を計画へ戻す。
> 完璧を強制せず、最終ゴールへ向かって反復する。

```text
Plan broadly.
Execute narrowly.
Learn from execution.
Update the plan.
Repeat.
```

---

# CバスFPGAボード固有の運用追記

- 本ファイルのMWP-Q手順を、このリポジトリの計画・実行規則として使用する。
- 計画の正本は `plan/` 以下に置く。全体像は `plan/master.md` から読む。
- FPGA品種、Cバス信号・タイミング、5 V入出力条件、レベル変換回路は、一次資料または実機測定で裏付けるまで「確定」と記述しない。
- Cバスへ接続する出力は、リセット中、FPGAコンフィグ中、クロック未確立時に安全なHigh-Zとなる設計を必須とする。
- RTL、回路図、PCB、ファームウェアの実装は、ユーザが承認した有限のQueueに含まれる範囲だけを行う。
- HDLシミュレーション、合成、実機試験のコマンドはツールチェーン確定後に各P書へ記録する。
- `plan/**/temp/` は破棄可能な一時領域とし、成果や検証証拠は `tests/` またはP書へ残す。

このプロトコルは Awe Morris の
[MWP-Q Agentic Coding Method](https://github.com/awemorris/MWP-Q-Agentic-Coding-Method)
（MIT License）を日本語版から取り込み、本プロジェクト向けに追記したものである。
ライセンス通知は `plan/LICENSE-MWP-Q` に収録する。
